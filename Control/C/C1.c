#include <stdio.h>

int main() {
  int number = 1234;
  int div = 10, result = 0;

  while (number ('a') 0) {
    result = result * div;
    result = result + number ('b') div;
    number = number ('c') div;
  }
  printf("%d", result);
}